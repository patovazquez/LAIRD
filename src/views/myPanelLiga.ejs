<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LAIRD</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/myPanel.css">
</head>

  <!-- header usuarios -->
  <%- include('Partials/userHeader') %>
  <!--fin del header usuarios -->
    <!-- header main -->
    <%- include('Partials/mainHeader') %>
    <!--fin del header main -->
  
<body>
    <a href="/images/res.pdf" download="/images/res.pdf">descarga</a> <!--eliminar esto lo use de prueba para el atr DOWNLOAD despues lo uso para los recursos -->
    <h3><%= liga.name %> </h3>

    <button class="mi-icono"><i class="fas fa-trash"></i></button>
    <button class="mi-icono"><i class="fas fa-pencil-alt"></i></button>
    <button class="mi-icono"><i class="fas fa-eye"></i></button>


    <% if (equipos.length == 0 ) { %>
        <h1>Todavia creaste ningun Equipo</h1>
       <% }else{ %>
        <h1>Tus equipos</h1>
   <% }%>

   <% equipos.forEach(equipo => { %>
    <h3><%= equipo.name %> </h3>
    <h4><%= equipo.description %></h4>

    <% }) %>    

    <a href="edit/<%= liga.id %>">
        <button class= "toDefine">Editar</button>    
    </a>

    <form action="delete/<%= liga.id %>?_method=DELETE" method="POST" enctype="multipart/form-data" id="delete">
        <button class= "generalButton">Eliminar</a></button>
        <!--<button class="mi-icono"><i class="fas fa-trash"></i></button>-->
    </form>

    <a href="../eventos/register">
        <button class= "toDefine">CREAR EVENTO</button>    
    </a>

    
</body>
</html>